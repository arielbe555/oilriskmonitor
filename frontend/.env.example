# Supabase (Phase 2 — backend uses service role key, frontend uses anon key)
VITE_SUPABASE_URL=https://panmgqtzqlpqubdeqxid.supabase.co
VITE_SUPABASE_ANON_KEY=

# Backend API (Phase 2)
VITE_API_URL=http://localhost:3000

# Mapbox (optional — currently using Leaflet/OpenStreetMap CartoDB DarkMatter, no token needed)
# VITE_MAPBOX_TOKEN=
